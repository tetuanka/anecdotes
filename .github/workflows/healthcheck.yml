name: Health check

#on:
#  schedule:
#    - cron:  '30 12 * * *'
on:
  push:
    branches:
      - main, master

jobs:
  health_check:
    runs-on: ubuntu-20.04
    steps:
      - name: Check the deployed service URL
        uses: jtalk/url-health-check-action@v3
        with:
          # Check the following URL
          url: https://routed-anecdotes.fly.dev/
 
